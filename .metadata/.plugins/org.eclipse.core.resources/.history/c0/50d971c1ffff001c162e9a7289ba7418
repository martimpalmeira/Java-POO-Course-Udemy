package exercises.secao19.practice228.model.service;

import java.util.ArrayList;

public class CalculationService<T extends Comparable> {

	public T max(ArrayList<T> list) {
		T maxP = list.get(0);
		for (T p : list) {
			if (maxP.compareTo(p) > 0) {
				maxP = p;
			}
		}return maxP;
	}
}
